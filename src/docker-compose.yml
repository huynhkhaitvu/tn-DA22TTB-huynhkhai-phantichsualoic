services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - ./backend:/app:delegated
      # Mount frontend into backend so Flask can serve the updated static files (CSV export changes)
      - ./frontend:/app/frontend:delegated
      - backend_data:/app/data
    environment:
      FLASK_ENV: development
      PYTHONUNBUFFERED: 1
      PYTHONPATH: /app
      SECRET_KEY: dev-secret-key-change-in-production
      FLASK_RUN_HOST: 0.0.0.0
    dns:
      - 8.8.8.8
      - 1.1.1.1
    env_file:
      - ./backend/.env
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "80:80"
    volumes:
      - ./frontend:/usr/share/nginx/html:delegated
    restart: unless-stopped

volumes:
  backend_data:

